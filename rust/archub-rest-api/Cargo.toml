[package]
name = "archub-rest-api"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
archub-core = { path = "../archub-core" }

[lib]
path = "src/lib.rs"
